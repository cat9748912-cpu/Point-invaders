Point Invaders is a browser-based, high-performance arcade network hub that bridges classic 2D neon vector designs with real-time 3D cyberpunk simulation mechanics. Developed as a comprehensive single-page web environment, the application integrates an expansive catalog of twenty-nine custom mini-games, an asynchronous multiplayer duel ecosystem, live scoreboards, and persistent progression loops. By bypassing heavy server-side handshakes in its guest state and utilizing localized state machine vectors, the platform provides immediate runtime execution directly inside the browser client environment. This document serves as the project's definitive README essay, detailing the engineering choices, structural patterns, and game execution models that define Point Invaders.   
The core differentiator of Point Invaders is its dynamic, hot-swappable rendering pipeline. Users can shift the entire interface between two visual modes instantly without requiring account logout or state disruption:2D Classic Mode: A flat, neon-accented vector aesthetic built to optimize CPU cycles and run seamlessly across lower-powered client hardware. It relies on explicit canvas or styled DOM nodes to maintain responsiveness.3D Cyberpunk Mode: An immersive, high-draw graphical tier introducing spatial depth, camera bloom, real-time lighting arrays, and chrome finishes. Designed to extract maximal performance from modern GPUs, it translates traditional 2D game loops into a fully volumetric layout.State Management and Guest ProgressionThe platform uses an un-authenticated, local-first progression model to eliminate onboarding friction. When an operative initiates a session as a guest, their points, credits, profile levels, and system upgrades are bound to the browser's persistent sandbox storage.This state can be permanently mirrored to a centralized ledger at any time via the Account Synchronization protocol, which wraps local variables into a unified username and password profile without resetting active multipliers or leaderboards.
The point generation loop in Point Invaders is governed by the System Stability configuration. Operatives can manually adjust the operational limits of the host runtime environment, trading stability for mechanical scaling vectors:Safe Mode & Stable Core: Standard multipliers (1.0x PTS) ensuring baseline difficulty curves and predictable game state updates.Overclock & Meltdown: High-risk variables that accelerate game-loop processing speed, introduce strict failure windows, and aggressively scale point distributions.This matrix interfaces directly with the Chaos Protocol, an optional modifier engine that injects random handicaps or windfalls before the execution of a mission, forcing players to dynamically alter their strategies mid-run.
The application establishes a closed-loop economy driven by two primary assets: Points (PTS) and Credits (CR). Points measure historical rank and tier placement, whereas Credits serve as the functional currency within the built-in Black Market module.A programmatic exchange interface allows operatives to convert assets at a fixed ratio (100 PTS = 1 CR) to acquire visual customizations, custom cursors, layout skins, world environment styles, and distinct audio packages known as Music Drive
Point Invaders splits its operational modes across several campaign vectors, including a twenty-chapter single-player campaign against "The Glitch," a multi-stage Boss Rush mode, a local multi-user Party Mode, and an Endless Protocol lane that removes spatial boundaries and score caps.The ecosystem features twenty-nine specialized game modules categorized by cognitive and mechanical intent:Reflex and Kinetic ExecutionClick Frenzy: A pure throughput test requiring rapid input registration within a constrained ten-second boundary.Neon Nebula: The signature vertical defense engine requiring continuous target acquisition against descending cluster waves.Dodge Cores & Coolant: Precision inertia simulators that challenge users to guide single nodes through bouncing firewall vectors and narrow cooling shafts.Reaction Time: A strict millisecond latency test monitoring state transitions across the core terminal screens.
Logic and Mainframe Decryption
Cyberpunk Tetris & Core Merge: Lattice compaction tools requiring players to drop matrix blocks or fuse data nodes to clear full code tracks before encountering absolute gridlock.Signal Trace: A non-reflexive, four-slot cipher deduction module relying on static feedback loops and boolean elimination arrays.Node Hacker: A memory-sequence playback matrix testing short-term data retention through structured node pulses.Defrag: A logic-based sector-clearing utility requiring proximity mapping based on neighboring system counts.Physics and Spatial StrategyCyber Pong: An adversarial, time-limited paddle physics module matching player velocity vectors against an AI controller.Grid Snake & Flappy Drone: Continuous momentum challenges focused on dynamic length expansion and column clearance within strict bounding frames.Ice Breaker & Ice Cutter: Stealth and deflection frameworks forcing operatives to navigate active security patrol cones and shatter encrypted defenses.Packet Sort & Server Stack: High-cadence organizational simulators tracking vertical stack slicing and rulesets that shift every twenty seconds.Network Arena and Asynchronous CommunicationThe collaborative and competitive core of the platform is hosted within the Network Arena. To maintain low execution latency, the arena supports multiple duel layouts (1v1 Duels, 2v2 Squads, and Co-Op Grids) using a unified synchronization interface:Quick Match: Drops the operative into the nearest available open node room or provisions a synthetic droid placeholder if the matching queue remains quiet.Room Codes: Generates a unique four-character hash identifier allowing users to link specific remote clients to the same instance vector.Replay Encoding: The engine contains a native telemetry tracker that stamps game inputs, time variables, and scores directly into exportable replay codes, facilitating cross-platform challenge tracking and validation.
